# 2026-05-27

## Conversación guardada (JP + Leeloo)

- Se retomó el problema de acceso a Chrome para control por OpenClaw.
- Diagnóstico: `browser profile=user` seguía fallando (timeouts/404) aun con Chrome levantado en `--remote-debugging-port=9222`.
- Se confirmó que Chrome sí exponía CDP (`http://127.0.0.1:9222/json/version`), pero el attach del perfil `user` era inestable.
- Se reinició gateway en varias ocasiones y se ejecutó `openclaw doctor --non-interactive` para revisar estado.
- Se ajustó configuración de `browser.profiles.user` (driver existing-session, cdpPort 9222, cambios de `userDataDir` entre temporal/personal) para pruebas.
- Se concluyó que el camino estable inmediato era usar perfil **openclaw** (aislado) para no bloquear trabajo.
- Se crearon accesos directos nuevos en escritorio:
  - `Chrome ON (OpenClaw).lnk`
  - `Chrome OFF (Normal).lnk`
  - `Chrome ON (Personal).lnk`
- Prueba funcional exitosa en entorno aislado:
  - JP inició sesión en Google Calendar.
  - Leeloo creó evento: **Tarea**, hoy 27 mayo, **3:00 PM–3:30 PM**.
  - JP confirmó que el evento apareció correctamente.
- Se acordó que más adelante se configurará flujo de resúmenes de correo por Telegram en nota de voz.
- JP pidió explícitamente guardar esta conversación.
