# 2026-04-22

- JP reportó fallas de autenticación OAuth con `openai-codex` (errores de refresh/callback) y pidió ayuda en español.
- Quedó claro que, cuando el login OAuth falla, una causa posible es flujo de callback local incompleto (`http://localhost:1455/auth/callback?...`) o conflicto del puerto local.
- Se dejó un checklist de renovación de auth en el escritorio de JP: `C:\Users\Usuario\OneDrive\Escritorio\Checklist_Renovar_Auth_OpenClaw.rtf`.
- JP confirmó que ya ejecutó `openclaw update` a la versión v2026.4.21 y estaba validando post-update (punto 3: prueba de mensaje/comando).
- Contexto de estilo útil: JP responde bien a guía paso a paso y tono directo/práctico.
- JP confirmó que el update quedó aparentemente estable y pidió explicación previa del paso 4 (`openclaw logs --follow`) antes de ejecutarlo.
- Tras correr logs, JP reportó muchos errores y pidió revisión directa del asistente en vez de pegar salida extensa.
- Evaluación comunicada: sin error grave inmediato; categorías observadas como no críticas en contexto: `refresh_token_reused` histórico, `handshake timeout`/`token_missing` de reconexión, `ENOENT` de `memory/*.md`, warning `punycode`.
- Riesgo a vigilar si reaparece: `Config write rejected ... size-drop` y `Gateway restart timed out` durante cambios de config/plugins, aunque con servicio saludable en ese momento.
